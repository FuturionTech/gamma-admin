/**
 * Dashboard mutations — intentionally empty.
 *
 * The gamma-api does not expose admin-level mutations for maintenance mode,
 * platform announcements, cache clearing, report export, or dashboard
 * preferences. Those stubs have been removed.
 */
